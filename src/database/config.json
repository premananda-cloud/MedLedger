{
  "_comment": "CypherAegis / MedLedger — External Configuration",

  "postgres": {
    "url_env": "DATABASE_URL",
    "_comment": "Connection string is built from DB_NAME, DB_HOST, DB_PASSWORD, DB_USER, DB_PORT in .env"
  },

  "jwt": {
    "secret_env": "JWT_SECRET",
    "secret_fallback": "dev-only-secret-change-before-any-real-use",
    "algorithm": "HS256",
    "expiration_hours": 1,
    "_comment": "secret_env is read first; fallback is used ONLY in dev/test mode."
  },

  "crypto": {
    "keygen_on_server": true,
    "_comment": "true = server generates keypair during registration (dev/test only). false = client must supply public_key_hex in the request body (production SSI model)."
  },

  "server": {
    "host": "0.0.0.0",
    "port": 8000,
    "env": "development",
    "allowed_origins": ["http://localhost:3000", "http://localhost:8000", "http://127.0.0.1:8000"]
  }
}
